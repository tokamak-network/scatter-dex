/** App-wide constants — single source of truth for branding and storage namespace. */

export const APP_NAME = 'zkScatterDEX';

/** Prefix for all AsyncStorage / SecureStore keys. */
export const STORAGE_NS = 'zkscatterdex';
