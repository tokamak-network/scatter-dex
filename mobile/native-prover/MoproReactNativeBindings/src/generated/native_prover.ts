// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
/* tslint:disable */
/* eslint-disable */
// @ts-nocheck
import nativeModule from "./native_prover-ffi";
import { type UniffiRustFutureContinuationCallback, type UniffiForeignFutureDroppedCallback, type UniffiForeignFutureDroppedCallbackStruct,
} from "./native_prover-ffi";
import { type UniffiByteArray, AbstractFfiConverterByteArray, FfiConverterArray, FfiConverterArrayBuffer, FfiConverterBool, FfiConverterInt32, FfiConverterMap, FfiConverterOptional, FfiConverterUInt8, RustBuffer, UniffiEnum, UniffiError, UniffiInternalError, UniffiRustCaller, uniffiCreateFfiConverterString, uniffiCreateRecord, uniffiTypeNameSymbol, variantOrdinalSymbol,
} from "uniffi-bindgen-react-native";
const uniffiCaller = new UniffiRustCaller(() => ({ code: 0 }));

const uniffiIsDebug =
  // @ts-ignore -- The process global might not be defined
  typeof process !== "object" ||
  // @ts-ignore -- The process global might not be defined
  process?.env?.NODE_ENV !== "production" ||
  false;

// Public interface members begin here.

export function generateCircomProof(zkeyPath: string, circuitInputs: string, proofLib: ProofLib): CircomProofResult /*throws*/ {
    return FfiConverterTypeCircomProofResult.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_generate_circom_proof(
        FfiConverterString.lower(zkeyPath),
        FfiConverterString.lower(circuitInputs),
        FfiConverterTypeProofLib.lower(proofLib),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function generateGnarkProof(r1csPath: string, pkPath: string, witnessJson: string): GnarkProofResult /*throws*/ {
    return FfiConverterTypeGnarkProofResult.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_generate_gnark_proof(
        FfiConverterString.lower(r1csPath),
        FfiConverterString.lower(pkPath),
        FfiConverterString.lower(witnessJson),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function generateHalo2Proof(srsPath: string, pkPath: string, circuitInputs: Map<string, Array<string>>): Halo2ProofResult /*throws*/ {
    return FfiConverterTypeHalo2ProofResult.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_generate_halo2_proof(
        FfiConverterString.lower(srsPath),
        FfiConverterString.lower(pkPath),
        FfiConverterMapStringSequenceString.lower(circuitInputs),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function generateNoirProof(circuitPath: string, srsPath: string | undefined, inputs: Array<string>, onChain: boolean, vk: ArrayBuffer, lowMemoryMode: boolean): ArrayBuffer /*throws*/ {
    return FfiConverterArrayBuffer.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_generate_noir_proof(
        FfiConverterString.lower(circuitPath),
        FfiConverterOptionalString.lower(srsPath),
        FfiConverterSequenceString.lower(inputs),
        FfiConverterBool.lower(onChain),
        FfiConverterArrayBuffer.lower(vk),
        FfiConverterBool.lower(lowMemoryMode),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function getNoirVerificationKey(circuitPath: string, srsPath: string | undefined, onChain: boolean, lowMemoryMode: boolean): ArrayBuffer /*throws*/ {
    return FfiConverterArrayBuffer.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_get_noir_verification_key(
        FfiConverterString.lower(circuitPath),
        FfiConverterOptionalString.lower(srsPath),
        FfiConverterBool.lower(onChain),
        FfiConverterBool.lower(lowMemoryMode),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

/**
 * You can also customize the bindings by #[uniffi::export]
 * Reference: https://mozilla.github.io/uniffi-rs/latest/proc_macro/index.html
 */
export function moproHelloWorld(): string {
    return FfiConverterString.lift(uniffiCaller.rustCall(
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_mopro_hello_world(
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function verifyCircomProof(zkeyPath: string, proofResult: CircomProofResult, proofLib: ProofLib): boolean /*throws*/ {
    return FfiConverterBool.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_verify_circom_proof(
        FfiConverterString.lower(zkeyPath),
        FfiConverterTypeCircomProofResult.lower(proofResult),
        FfiConverterTypeProofLib.lower(proofLib),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function verifyGnarkProof(r1csPath: string, vkPath: string, proofResult: GnarkProofResult): boolean /*throws*/ {
    return FfiConverterBool.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_verify_gnark_proof(
        FfiConverterString.lower(r1csPath),
        FfiConverterString.lower(vkPath),
        FfiConverterTypeGnarkProofResult.lower(proofResult),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function verifyHalo2Proof(srsPath: string, vkPath: string, proof: ArrayBuffer, publicInput: ArrayBuffer): boolean /*throws*/ {
    return FfiConverterBool.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_verify_halo2_proof(
        FfiConverterString.lower(srsPath),
        FfiConverterString.lower(vkPath),
        FfiConverterArrayBuffer.lower(proof),
        FfiConverterArrayBuffer.lower(publicInput),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

export function verifyNoirProof(circuitPath: string, proof: ArrayBuffer, onChain: boolean, vk: ArrayBuffer, lowMemoryMode: boolean): boolean /*throws*/ {
    return FfiConverterBool.lift(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_native_prover_fn_func_verify_noir_proof(
        FfiConverterString.lower(circuitPath),
        FfiConverterArrayBuffer.lower(proof),
        FfiConverterBool.lower(onChain),
        FfiConverterArrayBuffer.lower(vk),
        FfiConverterBool.lower(lowMemoryMode),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

// Hermes (React Native ≥ 0.74) ships TextEncoder and encodeInto, but not
// TextDecoder. For single-string decode (bytesToString), we polyfill via the
// C++ string_from_buffer helper using a duck-typed object matching the
// standard TextDecoder.decode signature. Once Hermes ships a real
// TextDecoder, the `typeof` check will pick it up automatically.
//
// For array-of-strings decode (readStringFromBuffer), we keep a dedicated C++
// helper: the polyfill path (new Uint8Array view + decode) measured ~40%
// slower on getStringArray benchmarks than a direct (buf, offset, length)
// call, due to the per-read view allocation and extra property lookups in
// string_from_buffer.
const stringConverter = (() => {
    const encoder = new TextEncoder();
    const decoder: { decode(input: UniffiByteArray): string } =
        typeof TextDecoder !== "undefined"
            ? new TextDecoder()
            : {
                  decode: (bytes: UniffiByteArray) =>
                      nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_from_buffer(
                          bytes,
                          undefined as any,
                      ) as string,
              };
    return {
        // Single-string lower() uses the C++ helper — TextEncoder.encode
        // measured ~43% slower on takeString benchmarks.
        stringToBytes: (s: string) =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_buffer(s, undefined as any),
        bytesToString: (ab: UniffiByteArray) => decoder.decode(ab),
        // Direct C++ call — bypasses uniffiCaller.rustCall() overhead.
        // Matters for N-element arrays.
        stringByteLength: (s: string) =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length(s, undefined as any) as number,
        // Encode directly into the RustBuffer backing store via
        // TextEncoder.encodeInto — zero intermediate allocation. Replaces
        // the old C++ write_string_into_buffer helper.
        writeStringIntoBuffer: (s: string, buf: any, offset: number): number => {
            const view = new Uint8Array(
                buf.arrayBuffer,
                offset,
                buf.arrayBuffer.byteLength - offset,
            );
            return encoder.encodeInto(s, view).written;
        },
        // Dedicated C++ helper — avoids per-read Uint8Array allocation and
        // the double property-lookup in string_from_buffer.
        readStringFromBuffer: (buf: any, offset: number, length: number): string =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__read_string_from_buffer(buf, offset, length) as string,
    };
})();
const FfiConverterString = uniffiCreateFfiConverterString(stringConverter);

export type G1 = {
    x: string,
    y: string,
    z: string
}

/**
 * Generated factory for {@link G1} record objects.
 */
export const G1 = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<G1, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<G1>,
    });
})();

const FfiConverterTypeG1 = (() => {
    type TypeName = G1;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                x: FfiConverterString.read(from), 
                y: FfiConverterString.read(from), 
                z: FfiConverterString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterString.write(value.x, into);
            FfiConverterString.write(value.y, into);
            FfiConverterString.write(value.z, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterString.allocationSize(value.x) +
             FfiConverterString.allocationSize(value.y) +
             FfiConverterString.allocationSize(value.z);
            
        }
    };
    return new FFIConverter();
})();

export type G2 = {
    x: Array<string>,
    y: Array<string>,
    z: Array<string>
}

/**
 * Generated factory for {@link G2} record objects.
 */
export const G2 = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<G2, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<G2>,
    });
})();

const FfiConverterTypeG2 = (() => {
    type TypeName = G2;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                x: FfiConverterSequenceString.read(from), 
                y: FfiConverterSequenceString.read(from), 
                z: FfiConverterSequenceString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterSequenceString.write(value.x, into);
            FfiConverterSequenceString.write(value.y, into);
            FfiConverterSequenceString.write(value.z, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterSequenceString.allocationSize(value.x) +
             FfiConverterSequenceString.allocationSize(value.y) +
             FfiConverterSequenceString.allocationSize(value.z);
            
        }
    };
    return new FFIConverter();
})();

export type CircomProof = {
    a: G1,
    b: G2,
    c: G1,
    protocol: string,
    curve: string
}

/**
 * Generated factory for {@link CircomProof} record objects.
 */
export const CircomProof = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<CircomProof, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<CircomProof>,
    });
})();

const FfiConverterTypeCircomProof = (() => {
    type TypeName = CircomProof;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                a: FfiConverterTypeG1.read(from), 
                b: FfiConverterTypeG2.read(from), 
                c: FfiConverterTypeG1.read(from), 
                protocol: FfiConverterString.read(from), 
                curve: FfiConverterString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterTypeG1.write(value.a, into);
            FfiConverterTypeG2.write(value.b, into);
            FfiConverterTypeG1.write(value.c, into);
            FfiConverterString.write(value.protocol, into);
            FfiConverterString.write(value.curve, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterTypeG1.allocationSize(value.a) +
             FfiConverterTypeG2.allocationSize(value.b) +
             FfiConverterTypeG1.allocationSize(value.c) +
             FfiConverterString.allocationSize(value.protocol) +
             FfiConverterString.allocationSize(value.curve);
            
        }
    };
    return new FFIConverter();
})();

export type CircomProofResult = {
    proof: CircomProof,
    inputs: Array<string>
}

/**
 * Generated factory for {@link CircomProofResult} record objects.
 */
export const CircomProofResult = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<CircomProofResult, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<CircomProofResult>,
    });
})();

const FfiConverterTypeCircomProofResult = (() => {
    type TypeName = CircomProofResult;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                proof: FfiConverterTypeCircomProof.read(from), 
                inputs: FfiConverterSequenceString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterTypeCircomProof.write(value.proof, into);
            FfiConverterSequenceString.write(value.inputs, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterTypeCircomProof.allocationSize(value.proof) +
             FfiConverterSequenceString.allocationSize(value.inputs);
            
        }
    };
    return new FFIConverter();
})();

export type GnarkProofResult = {
    proof: string,
    publicInputs: string
}

/**
 * Generated factory for {@link GnarkProofResult} record objects.
 */
export const GnarkProofResult = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<GnarkProofResult, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<GnarkProofResult>,
    });
})();

const FfiConverterTypeGnarkProofResult = (() => {
    type TypeName = GnarkProofResult;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                proof: FfiConverterString.read(from), 
                publicInputs: FfiConverterString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterString.write(value.proof, into);
            FfiConverterString.write(value.publicInputs, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterString.allocationSize(value.proof) +
             FfiConverterString.allocationSize(value.publicInputs);
            
        }
    };
    return new FFIConverter();
})();

export type Halo2ProofResult = {
    proof: ArrayBuffer,
    inputs: ArrayBuffer
}

/**
 * Generated factory for {@link Halo2ProofResult} record objects.
 */
export const Halo2ProofResult = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<Halo2ProofResult, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<Halo2ProofResult>,
    });
})();

const FfiConverterTypeHalo2ProofResult = (() => {
    type TypeName = Halo2ProofResult;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                proof: FfiConverterArrayBuffer.read(from), 
                inputs: FfiConverterArrayBuffer.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterArrayBuffer.write(value.proof, into);
            FfiConverterArrayBuffer.write(value.inputs, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterArrayBuffer.allocationSize(value.proof) +
             FfiConverterArrayBuffer.allocationSize(value.inputs);
            
        }
    };
    return new FFIConverter();
})();


// Error type: MoproError
export enum MoproError_Tags {
    CircomError = "CircomError",
    Halo2Error = "Halo2Error",
    NoirError = "NoirError",
    GnarkError = "GnarkError"
}
export const MoproError = (() => {

    type CircomError__interface = {
        tag: MoproError_Tags.CircomError;
        inner: 
Readonly<
[string
]>
    };
    class CircomError_ extends UniffiError implements CircomError__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "MoproError";
        readonly tag = MoproError_Tags.CircomError;
        readonly inner: 
Readonly<
[string
]>;
        constructor(v0: string) {
            super("MoproError", "CircomError");

            this.inner = Object.freeze([v0]);
        }

        static new(v0: string): CircomError_ {
            return new CircomError_(v0);
        }

        static instanceOf(obj: any): obj is CircomError_ {
            return obj.tag === MoproError_Tags.CircomError;
        }
        static hasInner(obj: any): obj is CircomError_ {
            return CircomError_.instanceOf(obj);
        }

        static getInner(obj: CircomError_): 
Readonly<
[string
]> {
            return obj.inner;
        }

    }

    type Halo2Error__interface = {
        tag: MoproError_Tags.Halo2Error;
        inner: 
Readonly<
[string
]>
    };
    class Halo2Error_ extends UniffiError implements Halo2Error__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "MoproError";
        readonly tag = MoproError_Tags.Halo2Error;
        readonly inner: 
Readonly<
[string
]>;
        constructor(v0: string) {
            super("MoproError", "Halo2Error");

            this.inner = Object.freeze([v0]);
        }

        static new(v0: string): Halo2Error_ {
            return new Halo2Error_(v0);
        }

        static instanceOf(obj: any): obj is Halo2Error_ {
            return obj.tag === MoproError_Tags.Halo2Error;
        }
        static hasInner(obj: any): obj is Halo2Error_ {
            return Halo2Error_.instanceOf(obj);
        }

        static getInner(obj: Halo2Error_): 
Readonly<
[string
]> {
            return obj.inner;
        }

    }

    type NoirError__interface = {
        tag: MoproError_Tags.NoirError;
        inner: 
Readonly<
[string
]>
    };
    class NoirError_ extends UniffiError implements NoirError__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "MoproError";
        readonly tag = MoproError_Tags.NoirError;
        readonly inner: 
Readonly<
[string
]>;
        constructor(v0: string) {
            super("MoproError", "NoirError");

            this.inner = Object.freeze([v0]);
        }

        static new(v0: string): NoirError_ {
            return new NoirError_(v0);
        }

        static instanceOf(obj: any): obj is NoirError_ {
            return obj.tag === MoproError_Tags.NoirError;
        }
        static hasInner(obj: any): obj is NoirError_ {
            return NoirError_.instanceOf(obj);
        }

        static getInner(obj: NoirError_): 
Readonly<
[string
]> {
            return obj.inner;
        }

    }

    type GnarkError__interface = {
        tag: MoproError_Tags.GnarkError;
        inner: 
Readonly<
[string
]>
    };
    class GnarkError_ extends UniffiError implements GnarkError__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "MoproError";
        readonly tag = MoproError_Tags.GnarkError;
        readonly inner: 
Readonly<
[string
]>;
        constructor(v0: string) {
            super("MoproError", "GnarkError");

            this.inner = Object.freeze([v0]);
        }

        static new(v0: string): GnarkError_ {
            return new GnarkError_(v0);
        }

        static instanceOf(obj: any): obj is GnarkError_ {
            return obj.tag === MoproError_Tags.GnarkError;
        }
        static hasInner(obj: any): obj is GnarkError_ {
            return GnarkError_.instanceOf(obj);
        }

        static getInner(obj: GnarkError_): 
Readonly<
[string
]> {
            return obj.inner;
        }

    }

    function instanceOf(obj: any): obj is MoproError {
        return obj[uniffiTypeNameSymbol] === "MoproError";
    }

    return Object.freeze({
        instanceOf,
  CircomError: CircomError_, 
  Halo2Error: Halo2Error_, 
  NoirError: NoirError_, 
  GnarkError: GnarkError_
    });

})();
export type MoproError = InstanceType<
    typeof MoproError['CircomError' | 'Halo2Error' | 'NoirError' | 'GnarkError']
>;

// FfiConverter for enum MoproError
const FfiConverterTypeMoproError = (() => {
    const ordinalConverter = FfiConverterInt32;
    type TypeName = MoproError;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            switch (ordinalConverter.read(from)) {
                case 1: return new MoproError.CircomError(FfiConverterString.read(from));
                case 2: return new MoproError.Halo2Error(FfiConverterString.read(from));
                case 3: return new MoproError.NoirError(FfiConverterString.read(from));
                case 4: return new MoproError.GnarkError(FfiConverterString.read(from));
                default: throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
        write(value: TypeName, into: RustBuffer): void {
            switch (value.tag) {
                case MoproError_Tags.CircomError: {
                    ordinalConverter.write(1, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner[0], into);
                    return;
                }
                case MoproError_Tags.Halo2Error: {
                    ordinalConverter.write(2, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner[0], into);
                    return;
                }
                case MoproError_Tags.NoirError: {
                    ordinalConverter.write(3, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner[0], into);
                    return;
                }
                case MoproError_Tags.GnarkError: {
                    ordinalConverter.write(4, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner[0], into);
                    return;
                }
                default:
                    // Throwing from here means that MoproError_Tags hasn't matched an ordinal.
                    throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
        allocationSize(value: TypeName): number {
            switch (value.tag) {
                case MoproError_Tags.CircomError: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(1);
                    size += FfiConverterString.allocationSize(inner[0]);
                    return size;
                }
                case MoproError_Tags.Halo2Error: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(2);
                    size += FfiConverterString.allocationSize(inner[0]);
                    return size;
                }
                case MoproError_Tags.NoirError: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(3);
                    size += FfiConverterString.allocationSize(inner[0]);
                    return size;
                }
                case MoproError_Tags.GnarkError: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(4);
                    size += FfiConverterString.allocationSize(inner[0]);
                    return size;
                }
                default: throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
    }
    return new FFIConverter();
})();

export enum ProofLib {
    Arkworks,
    Rapidsnark
}

const FfiConverterTypeProofLib = (() => {
    const ordinalConverter = FfiConverterInt32;
    type TypeName = ProofLib;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            switch (ordinalConverter.read(from)) {
                case 1: return ProofLib.Arkworks;
                case 2: return ProofLib.Rapidsnark;
                default: throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
        write(value: TypeName, into: RustBuffer): void {
            switch (value) {
                case ProofLib.Arkworks: return ordinalConverter.write(1, into);
                case ProofLib.Rapidsnark: return ordinalConverter.write(2, into);
            }
        }
        allocationSize(value: TypeName): number {
            return ordinalConverter.allocationSize(0);
        }
    }
    return new FFIConverter();
})();

// FfiConverter for Array<string>
const FfiConverterSequenceString = new FfiConverterArray(FfiConverterString);

// FfiConverter for Map<string, Array<string>>
const FfiConverterMapStringSequenceString = new FfiConverterMap(FfiConverterString, FfiConverterSequenceString);

// FfiConverter for string | undefined
const FfiConverterOptionalString = new FfiConverterOptional(FfiConverterString);


/**
 * This should be called before anything else.
 *
 * It is likely that this is being done for you by the library's `index.ts`.
 *
 * It checks versions of uniffi between when the Rust scaffolding was generated
 * and when the bindings were generated.
 *
 * It also initializes the machinery to enable Rust to talk back to Javascript.
 */
function uniffiEnsureInitialized() {
    // Get the bindings contract version from our ComponentInterface
    const bindingsContractVersion = 30;
    // Get the scaffolding contract version by calling the into the dylib
    const scaffoldingContractVersion = nativeModule().ubrn_ffi_native_prover_uniffi_contract_version();
    if (bindingsContractVersion !== scaffoldingContractVersion) {
        throw new UniffiInternalError.ContractVersionMismatch(scaffoldingContractVersion, bindingsContractVersion);
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_generate_circom_proof() !== 36118) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_generate_circom_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_generate_gnark_proof() !== 45818) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_generate_gnark_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_generate_halo2_proof() !== 20945) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_generate_halo2_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_generate_noir_proof() !== 13761) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_generate_noir_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_get_noir_verification_key() !== 40800) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_get_noir_verification_key");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_mopro_hello_world() !== 53713) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_mopro_hello_world");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_verify_circom_proof() !== 18666) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_verify_circom_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_verify_gnark_proof() !== 33709) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_verify_gnark_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_verify_halo2_proof() !== 10053) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_verify_halo2_proof");
    }
    if (nativeModule().ubrn_uniffi_native_prover_checksum_func_verify_noir_proof() !== 42351) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_native_prover_checksum_func_verify_noir_proof");
    }

    }

export default Object.freeze({
  initialize: uniffiEnsureInitialized,
  converters: {
    FfiConverterTypeCircomProof,
    FfiConverterTypeCircomProofResult,
    FfiConverterTypeG1,
    FfiConverterTypeG2,
    FfiConverterTypeGnarkProofResult,
    FfiConverterTypeHalo2ProofResult,
    FfiConverterTypeMoproError,
    FfiConverterTypeProofLib,
  }
});