# Function: updateRelayerInfo()

```ts
function updateRelayerInfo(
   registryAddress, 
   params, 
signer): Promise<TransactionResponse>;
```

Submit `updateInfo(url, fee)` — operator-self-service edit of
 endpoint URL + per-trade fee. Validates the fee range up front
 for the same UX reason `registerRelayer` does.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `registryAddress` | `string` |
| `params` | [`UpdateRelayerInfoParams`](../interfaces/UpdateRelayerInfoParams.mdx) |
| `signer` | `Signer` |

## Returns

`Promise`\<`TransactionResponse`\>
