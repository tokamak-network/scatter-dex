/* Auto-written by scripts/write-api-meta.mjs after `npm run api`.
 * Edit the script, not this file — typedoc's clean step removes
 * untracked entries on each rebuild. */
const meta = {
  index: "Overview",
  core: "core",
  contracts: "contracts",
  zk: "zk",
  relayer: "relayer",
  orderbook: "orderbook",
  notes: "notes",
  react: "react",
};

export default meta;
