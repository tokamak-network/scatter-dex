# Interface: SettleAuthFees

Per-side fee in token units the relayer charges, capped by the
 user's signed `maxFee` on each side.

## Properties

| Property | Type |
| ------ | ------ |
| <a id="feetokenmaker"></a> `feeTokenMaker` | `bigint` |
| <a id="feetokentaker"></a> `feeTokenTaker` | `bigint` |
