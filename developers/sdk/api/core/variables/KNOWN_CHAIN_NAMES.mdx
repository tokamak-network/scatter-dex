# Variable: KNOWN\_CHAIN\_NAMES

```ts
const KNOWN_CHAIN_NAMES: Record<number, string>;
```

Display names for chains zkScatter cares about. Unknown chains
 fall through to `Chain <id>` so the UI never blanks out.
