# Function: pairsByMarket()

```ts
function pairsByMarket(pairs?): Record<QuoteMarket, WhitelistedPair[]>;
```

Group pairs by quote market for Upbit-style tabs. The returned
 map has stable insertion order matching the original pair list.

## Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `pairs` | readonly [`WhitelistedPair`](../interfaces/WhitelistedPair.mdx)[] | `LAUNCH_PAIRS` |

## Returns

`Record`\<[`QuoteMarket`](../type-aliases/QuoteMarket.mdx), [`WhitelistedPair`](../interfaces/WhitelistedPair.mdx)[]\>
