# Interface: CircuitAssets

Wasm + zkey assets for a single circuit. snarkjs accepts URLs,
 ArrayBuffers, or Uint8Arrays for both — pass whatever the host
 has already loaded.

## Properties

| Property | Type |
| ------ | ------ |
| <a id="wasm"></a> `wasm` | `string` \| `ArrayBuffer` \| `Uint8Array`\<`ArrayBufferLike`\> |
| <a id="zkey"></a> `zkey` | `string` \| `ArrayBuffer` \| `Uint8Array`\<`ArrayBufferLike`\> |
