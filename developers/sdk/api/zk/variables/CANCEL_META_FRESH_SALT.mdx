# Variable: CANCEL\_META\_FRESH\_SALT

```ts
const CANCEL_META_FRESH_SALT: "freshSalt";
```

Meta-channel key under which `cancel.worker` returns the
 private freshSalt to `assembleCancelProofResult`.
