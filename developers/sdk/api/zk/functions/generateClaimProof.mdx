# Function: generateClaimProof()

```ts
function generateClaimProof(input, assets): Promise<ClaimProofResult>;
```

Generate a Groth16 claim proof for one slot of a settlement.

 Pre-checks:
 - `leafIndex` in range (slow path) or non-negative (fast path)
 - claim data hashes to the leaf at `leafIndex` (slow path) —
   catches "wrong claim file" / "wrong settlement" mistakes
   loudly instead of after a 2 s proof
 - `allClaimLeaves.length === 2^CLAIMS_TREE_DEPTH`

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | [`ClaimProofInput`](../interfaces/ClaimProofInput.mdx) |
| `assets` | [`CircuitAssets`](../interfaces/CircuitAssets.mdx) |

## Returns

`Promise`\<[`ClaimProofResult`](../interfaces/ClaimProofResult.mdx)\>
