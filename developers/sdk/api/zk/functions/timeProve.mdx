# Function: timeProve()

```ts
function timeProve<T>(circuit, run): Promise<T>;
```

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `circuit` | [`ZkCircuit`](../type-aliases/ZkCircuit.mdx) |
| `run` | () => `Promise`\<`T`\> |

## Returns

`Promise`\<`T`\>
