---
title: Relayer
description: "HTTP API every zkScatter relayer node exposes. Auto-generated from the relayer's zod schemas."
---

{/* This file exists only to register the route in the Nextra sidebar.
    The actual page is rendered by `apps/docs/app/rest/relayer/page.tsx`,
    which Next.js routes to ahead of the catch-all when /rest/relayer
    is requested. The body below is ignored at runtime. */}
